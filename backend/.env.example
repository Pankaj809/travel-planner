# aizex.top API key (OpenAI-compatible) - used for both the chat models
# (gpt-4o / gpt-4o-mini) and the text-embedding-3-small embedding model.
LLM_API_KEY=

# All variables below are optional - config.py falls back to the defaults
# shown here if unset.
# LLM_BASE_URL=https://aizex.top/v1
# EMBEDDING_MODEL=text-embedding-3-small
# ORCHESTRATOR_MODEL=gpt-4o
# WORKER_MODEL=gpt-4o-mini
# CHROMA_DIR_PATH=./chroma
# CHROMA_COLLECTION_NAME=chromadb
